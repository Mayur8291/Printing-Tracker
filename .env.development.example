# Local Vite dev server — STAGING Supabase only (loaded automatically by `npm run dev`)
# Copy to .env.development (gitignored). Never put production keys here.

VITE_SUPABASE_URL=https://scvojtvgnkmbupvyslmb.supabase.co
VITE_SUPABASE_ANON_KEY=your-staging-anon-key-from-supabase-dashboard

# After Netlify builds branch develop, paste deploy URL here (Admin → Open hosted staging):
# VITE_STAGING_SITE_URL=https://develop--printingtracker.netlify.app
