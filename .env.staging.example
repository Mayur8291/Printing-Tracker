# Used by `npm run dev:staging` (Vite mode: staging)
# Copy to .env.staging (gitignored) if you want an explicit staging mode separate from development.

VITE_SUPABASE_URL=https://YOUR_STAGING_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your-staging-anon-or-publishable-key
